<% if product.images %>
  <% product.all_images.each do |image| %>
    <% Rails.logger.debug image.inspect %>
    <%= image_tag(image[0] ,alt: image[1],class:'image',size: image[2] )%>
  <% end %>
<% end %>